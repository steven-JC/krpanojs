export default class Krpano {
    /**
     * 加载漫游xml
     * @param {
     *  @xmlStringOrPath: string
     *  @sceneName: string
     *  @vars: IObject
     *  @flags: Flag[]
     *  @blend: string
     * } param0
     */
    public load({
        xmlStringOrPath,
        sceneName,
        blend
    }: {
        xmlStringOrPath: string
        sceneName: string
        blend: string
    }) {
        console.log(11)
    }
}
